# Practice!
# 1. Try removing `sensitive = true` from the output block in outputs.tf.
#    Run `terraform apply` and see the error message Terraform gives you.
#    (It protects you from accidentally exposing sensitive vars!)
