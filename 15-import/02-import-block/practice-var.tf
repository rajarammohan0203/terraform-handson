# Practice!
# 1. Create a different resource (e.g. SQS Queue) manually.
#    aws --endpoint-url=http://localhost:4566 sqs create-queue --queue-name my-manual-queue
# 2. Add an import block for it in main.tf.
# 3. Regenerate the config.
