# PRACTICE CHALLENGE
# 1. Run `terraform init`.
#    (CRITICAL: You MUST run init whenever you add/change a module).
# 2. Run `terraform apply`.
# 3. Observe: Terraform creates 2 buckets using the logic from one file.
# 4. Verify in LocalStack: `awslocal s3 ls`.
# 5. Try modifying the Child Module (e.g., add a tag).
# 6. Run `terraform apply`. Both buckets will get the update!
