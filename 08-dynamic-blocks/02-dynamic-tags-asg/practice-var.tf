# Practice!
# 1. Add a new tag to the variable `custom_tags` in variables.tf.
# 2. Run plan and see the new block generated.
# 3. Advanced: Try to make `propagate_at_launch` dynamic too!
#    (Hint: Change the variable type to map(object({ value=string, propagate=bool }))).
