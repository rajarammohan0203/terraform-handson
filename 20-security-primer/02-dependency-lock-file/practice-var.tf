# Practice!
# 1. Commit the .terraform.lock.hcl file to git (Conceptually).
# 2. Delete the .terraform/ folder (simulate a fresh CI run).
# 3. Run `terraform init`. It will rely on the lock file to verify the download.
